
"use client";

// This component's logic has been moved to src/contexts/LoadingContext.tsx
// to resolve a Next.js module loading error. This file is now a placeholder
// and can be safely deleted in a future step if desired.

// We export a dummy component to prevent breaking any imports that might still exist.
export default function AvatarLoadingOverlay() {
  return null;
}
